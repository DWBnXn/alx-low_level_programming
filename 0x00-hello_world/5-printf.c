#include <stdio.h>

/**
 * main- Serves as entry point for the program
 * Return: By defaulf zero if successful otherwise a non-zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
